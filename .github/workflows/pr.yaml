name: PR

on:
    pull_request:

jobs:
    check:
        name: Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - uses: valitydev/action-frontend/setup-install@v3
            - name: Check
              run: npm run build
